{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the companion's own window may do. Everything else stays in Rust.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
